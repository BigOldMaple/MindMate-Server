diff --git a/node_modules/expo-modules-core/android/build.gradle b/node_modules/expo-modules-core/android/build.gradle
index original..updated 100644
--- a/node_modules/expo-modules-core/android/build.gradle
+++ b/node_modules/expo-modules-core/android/build.gradle
@@ -115,3 +115,9 @@
   def appPathAsFile = property("expo.app.path")?.with { new File(it) } ?: appPathAsText?.with { new File(it) }
   return appPathAsFile?.exists() ? appPathAsFile : null
 }
+
+// Add Kotlin compiler options to skip metadata version checks
+tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
+  kotlinOptions {
+    freeCompilerArgs += ["-Xskip-metadata-version-check", "-Xsuppress-version-warnings"]
+  }
+}